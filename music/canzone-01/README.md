# canzone-01

Carica qui gli stem di **una sola canzone** (m4a/mp3/wav).
L'app sceglie una cartella a caso a ogni partita e usa solo i suoi file.

BPM: aggiungi un file vuoto chiamato ad es. `120bpm.txt` per impostare il tempo del brano (default 120).
